interface:
  display_name: "Repo Pruner"
  short_description: "Detection-only repository health evidence"
  default_prompt: "Use $repo-pruner to prepare deterministic repository-health evidence and stop before remediation."
policy:
  allow_implicit_invocation: false
