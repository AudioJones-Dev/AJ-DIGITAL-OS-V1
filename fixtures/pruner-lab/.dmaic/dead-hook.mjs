// DECOY: deliberately unreferenced governance hook.
// Every top-level dot-directory is protected before detector classification.
export function onComponentStatusChange(componentId) {
  return { componentId, observed: true };
}
